version: "3.3"

services:
  app:
    container_name: pablo-coder
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /etc/hosts:/usr/src/app  
    command: bash start.sh
    restart: on-failure
    ports:
      - "80:80"
